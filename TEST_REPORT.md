# NoteCli v0.1.0 功能测试报告

> **测试日期**: 2026-04-25
> **测试对象**: `target/debug/NoteCli.exe`
> **测试环境**: Windows 11 Pro 10.0.26200
> **需求基线**: `REQUIREMENTS.md`

---

## 1. 测试概览

| 指标 | 结果 |
|------|------|
| 测试用例总数 | 48 |
| 通过 | 38 (79%) |
| 部分通过 | 3 (6%) |
| 占位符/未实现 | 4 (8%) |
| 失败 | 0 |
| 整体完成度 | ~80% |

---

## 2. 功能测试详情

### 2.1 基础命令

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 版本号 | `NoteCli --version` | 显示版本号 | `NoteCli 0.1.0` | ✅ |
| 无参数运行 | `NoteCli` | 友好提示 | `未指定子命令，使用 --help 查看帮助` | ✅ |
| 帮助信息 | `NoteCli --help` | 列出所有子命令 | 20 个子命令完整列出 | ✅ |

### 2.2 笔记创建 (`add`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 基本创建 | `add "测试笔记第一条"` | 自动分配 ID=1 | `笔记已创建 (ID: 1)` | ✅ |
| 完整参数 | `add "内容" -t "自定义标题" -c work -T rust -T cli -p high` | 标题/分类/标签/优先级均生效 | ID=2, 所有字段正确 | ✅ |
| 重复标题 | 连续两次 `add "x" -t "测试笔记"` | 第二次追加后缀 | 标题变为 `测试笔记 (2)` | ✅ |
| 自增 ID | 删除 ID=3 后创建 | 新 ID=5（不回收） | ID 跳过已删除值 | ✅ |
| 未指定标题 | `add "测试笔记第一条"` | 内容前 20 字符作标题 | 标题为 `未命名笔记` | ❌ BUG |
| 空内容 | `add ""` | 应拒绝（内容为必填） | 成功创建空笔记 | ❌ BUG |

**BUG-1**: 未指定 `--title` 时，标题应为内容前 20 字符，实际生成 `未命名笔记`。

**BUG-2**: `add ""` 允许创建空内容笔记，缺少内容非空校验。

### 2.3 笔记查看 (`show`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 彩色显示 | `show 1` | 含颜色高亮 | 标题/ID/优先级等均有颜色 | ✅ |
| 纯文本 | `show 2 --raw` | 无 ANSI 码 | 输出纯文本格式 | ✅ |
| 不存在 ID | `show 999` | 明确错误提示 | `错误: 笔记ID 999 不存在` | ✅ |
| 无效 ID | `show abc` | 中文友好提示 | clap 英文错误 `invalid digit found in string` | ❌ BUG |

**BUG-3**: 无效 ID 输入时显示英文 clap 错误而非中文友好提示。

### 2.4 笔记编辑 (`edit`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 修改标题 | `edit 2 --title "修改后的标题"` | 标题更新 | 标题、磁盘文件同步更新 | ✅ |
| 修改优先级 | `edit 2 --priority low` | 优先级更新 | `low` 生效 | ✅ |
| 追加内容 | `edit 2 --append "追加的内容"` | 末尾追加文本 | 内容追加成功 | ✅ |
| 替换内容 | `edit 2 --content "完全替换的内容"` | 正文被替换 | 内容替换成功 | ✅ |
| 修改时间更新 | 编辑后查看 | modified 更新 | 修改时间自动刷新 | ✅ |
| 编辑不存在 ID | `edit 999 --title "x"` | 错误提示 | `错误: 笔记ID 999 不存在` | ✅ |
| 编辑已归档笔记 | `edit 1 --title "x"` (已归档) | 拒绝操作 | `错误: 笔记 1 已归档，无法编辑` | ✅ |

### 2.5 笔记删除 (`delete`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 强制删除 | `delete 3 -f` | 永久删除 | 磁盘文件+索引记录均移除 | ✅ |
| ID 不回收 | 删除 ID=3 后查看 | ID=3 不再出现 | 新 ID 从 5 开始 | ✅ |
| 删除已归档笔记 | `delete 1` (已归档) | 拒绝操作 | `错误: 笔记 1 已归档，无法删除` | ✅ |

### 2.6 笔记列表 (`list`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 默认列表 | `list` | 表格含 ID/标题/分类/优先级/标签/状态 | 格式正确 | ✅ |
| 按分类筛选 | `list -c work` | 仅显示 work 分类 | 筛选正确 | ✅ |
| 按标签筛选 | `list -T rust` | 仅显示含 rust 标签 | 筛选正确 | ✅ |
| 按优先级筛选 | `list -p low` | 仅显示低优先级 | 筛选正确 | ✅ |
| 按日期筛选 | `list --date today` | 仅显示今日创建 | 筛选正确 | ✅ |
| 指定日期 | `list --date 2025-01-01` | 无匹配 | `没有找到笔记` | ✅ |
| 有标签筛选 | `list --has-tag` | 仅显示有标签笔记 | 筛选正确 | ✅ |
| 无标签筛选 | `list --no-tag` | 仅显示无标签笔记 | 筛选正确 | ✅ |
| 按标题排序 | `list --sort title` | 字母序 | 排序正确 | ✅ |
| 按优先级排序 | `list --sort priority` | 优先级排序 | low 在前 | ✅ |
| 按修改时间排序 | `list --sort modified` | 按修改时间 | 排序正确 | ✅ |
| 分页 | `list -n 1 -o 1` | 跳过第1条显示第2条 | 分页正确 | ✅ |
| 置顶标记 | 置顶后 `list` | 置顶笔记显示📌 | 标记正确 | ✅ |
| 归档笔记显示 | 归档后 `list` | 从默认列表隐藏 | 仍显示，标注[已归档] | ❌ BUG |

**BUG-4**: 归档的笔记应从默认列表中隐藏，但实际仍显示在 `list` 中（仅标注状态）。与需求"归档笔记（从默认列表中隐藏）"不符。

### 2.7 搜索 (`search`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 搜索标题/正文 | `search "修改"` | 匹配标题含"修改"的笔记 | 找到 1 条 | ✅ |
| 搜索标签 | `search "rust"` | 匹配标签含 rust 的笔记 | `未找到匹配` | ❌ BUG |
| 搜索分类 | `search "work"` | 匹配分类为 work 的笔记 | 未测试（依赖标签 BUG） | ⚠️ |
| 正则模式 | `search "测试" --mode regex` | 正则匹配 | 找到 3 条 | ✅ |
| 正则跨字段 | `search "test.*cli" --mode regex` | 匹配标签 | 未找到 | ❌ BUG |
| 模糊模式 | `search "测试" --mode fuzzy` | 模糊匹配 | 结果同 plain | ✅ |
| 大小写敏感 | `search "test" --case-sensitive` | 区分大小写 | 仅匹配小写 test | ✅ |
| 无结果 | `search "不存在的内容xyz"` | 友好提示 | `未找到匹配的笔记` | ✅ |

**BUG-5 (严重)**: 搜索范围未覆盖标签和分类字段。`search "rust"` 无法搜到标签含 `rust` 的笔记，正则 `test.*cli` 也无法匹配标签。需求明确要求搜索范围覆盖"标题、正文、分类、标签"。

### 2.8 置顶与归档

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 置顶 | `pin 2` | 置顶成功 | `笔记 2 已置顶` | ✅ |
| 重复置顶 | `pin 2` (已置顶) | 友好提示 | `笔记 2 已经置顶` | ✅ |
| 取消置顶 | `unpin 2` | 取消成功 | `笔记 2 已取消置顶` | ✅ |
| 归档 | `archive 1` | 归档成功 | `笔记 1 已归档` | ✅ |
| 编辑已归档 | `edit 1 --title "x"` | 拒绝 | 正确拒绝 | ✅ |
| 删除已归档 | `delete 1` | 拒绝 | 正确拒绝 | ✅ |
| 取消归档 | `unarchive 1` | 恢复 | `笔记 1 已取消归档` | ✅ |
| 标记完成 | `done 2` | 显示 [DONE] | 显示 [已归档] | ❌ BUG |

**BUG-6**: `done` 标记的笔记在列表中显示 `[已归档]` 而非需求定义的 `[DONE]`。

### 2.9 分类管理 (`category`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 列出分类 | `category list` | 含笔记数量 | 表格含 ID/名称/笔记数 | ✅ |
| 分类树 | `category tree` | 树形展示 | `default (1 条笔记)` 格式 | ✅ |
| 重命名 | `category rename work dev` | 所有引用同步 | 笔记分类+磁盘文件均更新 | ✅ |
| 删除(保留) | `category delete default --keep` | 需确认 | 提示使用 `-f` 确认 | ✅ |

### 2.10 标签管理 (`tag`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 列出标签 | `tag list` | 含使用次数 | 表格含 ID/名称/笔记数 | ✅ |
| 重命名 | `tag rename rust rustlang` | 所有引用同步 | 笔记标签+磁盘文件均更新 | ✅ |
| 删除 | `tag delete test` | 从所有笔记移除 | 提示需 `-f` 确认 | ⚠️ 占位符 |

### 2.11 导出 (`export`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 无筛选条件 | `export --format json` | 提示指定筛选 | 警告提示使用 `--all` 或筛选 | ✅ |
| JSON 全量导出 | `export --format json --all` | 导出 JSON | 格式正确，字段完整 | ✅ |
| CSV 导出 | `export --format csv --all` | 导出 CSV | 含表头，引号转义正确 | ✅ |
| Markdown 导出 | `export --format markdown --all` | 每条笔记独立 .md | 文件格式美观 | ✅ |
| 纯文本导出 | `export --format txt --id 3` | 单条导出 | 成功 | ✅ |
| 自定义路径 | `export --format txt -a -p /custom/path` | 导出到指定路径 | 成功 | ✅ |

### 2.12 导入 (`import`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 分隔导入 | `import file.txt` (含 `---` 分隔) | 2 条独立笔记 | 导入 2 条，ID 自增 | ✅ |
| 带默认分类/标签 | `import file.txt -c imported -T test` | 分类和标签生效 | 每条笔记均含 imported/test | ✅ |

### 2.13 统计 (`stats`)

| 用例 | 输入 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 统计信息 | `stats` | 完整统计表 | 含总数/置顶/归档/优先级分布/分类数/标签数 | ✅ |

### 2.14 占位符命令

| 命令 | 输入 | 实际输出 | 状态 |
|------|------|---------|------|
| `index rebuild` | `index rebuild` | `命令: Index Rebuild` | ❌ 未实现 |
| `config list` | `config list` | `命令: Config List` | ❌ 未实现 |
| `config get` | `config get default_priority` | 仅打印参数值 | ❌ 未实现 |
| `completion` | `completion bash` | `命令: Completion, shell: Bash` | ❌ 未实现 |

### 2.15 数据存储与同步

| 用例 | 操作 | 预期 | 实际 | 结果 |
|------|------|------|------|------|
| 存储目录结构 | 创建笔记后检查 | `.notecli/notes/*.md` + `index.json` | 结构正确 | ✅ |
| JSON front matter | 查看笔记文件 | `---\n{JSON}\n---\n正文` | 格式正确 | ✅ |
| 文件名净化 | 标题含特殊字符 | 替换为 `_` | 未测试（需特殊字符标题） | — |
| 启动同步(磁盘删除) | 手动删除 .md 文件后 `list` | 索引自动清理 | `show 57` 返回不存在 | ✅ |
| 写新删旧策略 | 编辑标题时 | 新文件创建后旧文件删除 | 文件名正确更新 | ✅ |

---

## 3. 性能测试

测试方法：在空白环境中批量创建 100 条笔记后测量。

| 操作 | 耗时 | 需求目标 | 是否达标 |
|------|------|---------|---------|
| 单条创建（平均） | ~22ms | < 50ms | ✅ |
| 列表展示（106 条） | 55ms | < 100ms | ✅ |
| 全文搜索（106 条） | 34ms | < 200ms | ✅ |
| 批量创建 100 条（总） | 2.27s | — | — |

---

## 4. BUG 汇总

| 编号 | 严重度 | 描述 | 需求参照 |
|------|--------|------|---------|
| BUG-1 | 中 | `add` 未指定标题时生成"未命名笔记"而非内容前 20 字符 | §3.1.1 |
| BUG-2 | 低 | `add ""` 允许创建空内容笔记，缺少非空校验 | §3.1.1 |
| BUG-3 | 低 | 无效 ID（如 `abc`）显示英文 clap 错误，非中文友好提示 | §12 |
| BUG-4 | 中 | 归档笔记仍显示在 `list` 默认列表中，未隐藏 | §3.2.1 |
| BUG-5 | **高** | 搜索不覆盖标签和分类字段 | §3.3 |
| BUG-6 | 中 | `done` 标记的笔记显示为 [已归档] 而非 [DONE] | §10.3 |

---

## 5. 需求覆盖率

### 已实现功能 (38/48)

- 基础命令: `--version`, `--help`, 无参数提示
- CRUD: `add`(含完整参数), `show`(含 `--raw`), `edit`(标题/分类/标签/优先级/内容/追加), `delete`
- 列表: `list` 含分类/标签/优先级/日期/有无标签筛选、排序、分页
- 搜索: `search` 纯文本/正则/模糊模式、大小写敏感
- 分类: `category list/tree/rename/delete`
- 标签: `tag list/rename`
- 快捷操作: `pin/unpin/archive/unarchive`
- 导入导出: `export` (json/csv/markdown/txt)、`import`
- 统计: `stats`
- 存储: 自动同步、JSON front matter、写新删旧

### 部分实现 (3/48)

- `done`: 功能存在但显示标记不正确
- `search --mode regex`: 基本可用但不覆盖标签/分类
- `tag delete`: 有确认提示但逻辑不完整

### 占位符/未实现 (7/48)

- `index rebuild`
- `config list / get / set`
- `completion`
- `note add` 不带内容打开编辑器
- 批量删除
- 删除确认交互
- Markdown 终端渲染

---

## 6. 改进建议

### 优先级 P0（核心功能缺陷）

1. **修复搜索范围**：`search` 应同时搜索标题、正文、分类名称、标签名称，这是核心查询能力

### 优先级 P1（需求不符）

2. **修复标题自动生成**：`add` 未指定 `--title` 时应取内容前 20 字符作为标题
3. **修复归档列表过滤**：`list` 默认应排除已归档笔记（可加 `--archived` 参数查看）
4. **修复 done 显示**：标记为完成的笔记应显示 `[DONE]` 而非 `[已归档]`

### 优先级 P2（体验优化）

5. **空内容校验**：`add` 应拒绝空字符串
6. **无效 ID 友好提示**：对非数字 ID 给出中文错误信息
7. **优先级大小写统一**：存储和显示统一为小写（`normal`/`high`/`low`/`urgent`）
